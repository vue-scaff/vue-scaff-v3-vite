export default ({}) => {
  const state = {
    alive: ['start'],
    oss: `http://oss.joenix.com/vue-scaff`,
  };

  const mutations = {};

  const actions = {};

  return { state, mutations, actions };
};
